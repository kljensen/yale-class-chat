<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :score %>
  <%= number_input f, :score %>
  <%= error_tag f, :score %>
  <%= error_tag f, :one_rating_per_user %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
