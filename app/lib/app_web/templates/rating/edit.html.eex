<h1>Edit Rating</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.rating_path(@conn, :update, @rating)) %>

<span><%= link "Back", to: Routes.submission_rating_path(@conn, :index, @submission) %></span>
