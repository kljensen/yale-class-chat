<h1>Edit Course</h1>
<p>Note: semester cannot be changed once a course is created</p>

<%= render "form.html", Map.put(assigns, :action, Routes.course_path(@conn, :update, @course)) %>

<span><%= link "Back", to: Routes.course_path(@conn, :index) %></span>
