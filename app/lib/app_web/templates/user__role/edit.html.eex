<h1>Edit User  role</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user__role_path(@conn, :update, @user__role)) %>

<span><%= link "Back", to: Routes.user__role_path(@conn, :index) %></span>
