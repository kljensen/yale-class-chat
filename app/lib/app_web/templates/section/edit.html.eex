<h1>Edit Section in <%= @course.name %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.section_path(@conn, :update, @section)) %>

<span><%= link "Back", to: Routes.course_path(@conn, :show, @section.course_id) %></span>
