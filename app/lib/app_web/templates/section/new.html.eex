<h1>New Section in <%= @course.name %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.section_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.course_path(@conn, :show, @course.id) %></span>
