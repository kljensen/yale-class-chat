<h1>New Comment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.submission_comment_path(@conn, :create, @submission)) %>

<span><%= link "Back", to: Routes.submission_comment_path(@conn, :index, @submission) %></span>
